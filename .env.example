# Copy to .env and set values. Do not commit .env.
# Fully local stack: Postgres, Redis, and MongoDB run in Docker Compose.

# Required for Docker Compose (backend gets DATABASE_URL etc. from Compose)
POSTGRES_USER=app
POSTGRES_PASSWORD=set-a-secure-password
POSTGRES_DB=quotes
JWT_SECRET=set-a-secret-at-least-32-chars

# For running backend/frontend on host (not in Docker), set these to reach local containers:
# DATABASE_URL=postgres://app:yourpassword@localhost:5432/quotes
# REDIS_URL=redis://localhost:6379
# MONGODB_URI=mongodb://localhost:27017/content

# Optional – defaults shown
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3001
# VITE_PUBLIC_API_BASE_URL=http://localhost:3002
# BACKEND_PORT=3001
# FRONTEND_PORT=3000
# POSTGRES_PORT=5432
# REDIS_PORT=6379
# REDIS_PUBLIC_PORT=6381
# DB_POOL_MAX=20

# Public API (separate backend service, port 3002)
# PUBLIC_API_PORT=3002
# PUBLIC_REDIS_URL=redis://localhost:6381
# PUBLIC_DB_POOL_MAX=10
# DATABASE_URL_PUBLIC=  (optional read-only Postgres user)
# MONGODB_URI_PUBLIC=   (optional read replica / read-only Mongo)
# PUBLIC_API_BASE_URL=  (for Swagger server URL)
