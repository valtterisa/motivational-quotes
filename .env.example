# Required for docker compose (and for local backend/frontend if you run them outside Docker).
# Copy to .env and set real values. Do not commit .env.

# Database (must match between db and backend).
# If you get "password authentication failed", the DB volume was likely created with a different password.
# Fix: docker compose down -v && docker compose up (wipes DB; then Postgres uses the password below).
POSTGRES_USER=app
POSTGRES_PASSWORD=set-a-strong-password
POSTGRES_DB=quotes

# Backend auth – use a long random string in production (e.g. openssl rand -base64 32)
JWT_SECRET=set-a-secret-at-least-32-chars

# Optional – read replica for feed (if unset, feed uses primary)
# DATABASE_READ_URL=postgres://app:password@postgres-replica:5432/quotes

# Optional – connection pool size (default 10)
# DB_POOL_MAX=20

# Optional – defaults shown (override in .env if needed)
# POSTGRES_USER=app
# POSTGRES_DB=quotes
# BACKEND_PORT=3001
# FRONTEND_PORT=3000
# REDIS_PORT=6380
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3001
